{#
/**
 * @file
 * Default theme implementation to present a website feedback entity.
 *
 * This template is used when viewing a registered website feedback's page,
 * e.g., /admin/content/website-feedback)/123. 123 being the website feedback's ID.
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 *   print a subset such as 'content.title'.
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_website_feedback()
 */
#}
<article{{ attributes }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</article>
